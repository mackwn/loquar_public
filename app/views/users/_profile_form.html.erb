<%= form_for(:user, url: update_profile_path, remote: true) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :role %>
    <%= f.text_area :role %>
  </div>
  
  <div class="field">
    <%= f.label :level %>
    <%= f.number_field :level %>
  </div>
  
  <div class="field">
    <%= f.label :points %>
    <%= f.number_field :points %>
  </div>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
